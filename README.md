# Vetum

Agents propose. Policy decides. Binance executes.

Programmable control plane for Binance Agent OS. Not a trading bot.

## Status

Chunk 1 (constitution) is done. `npm test` — 9/9.

Next: Chunk 2 (Chamber UI). Read `memory.md` first.

## Docs for humans and other LLMs

- `memory.md` — where we stopped, what to do next
- `project.context.md` — full product, rules, rubric, UI law
- `BUILD.md` — five chunks and done-checks

## Run

```bash
npm test
```

Node 20+.
