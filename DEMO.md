# Demo shot list (~90s, 2 min max)

1920×1080. Start on a stamp or a tool call, not the orb.

## 0:00–0:25  ChatGPT (VetumAgent project, Binance plugin ON)

> VetumAgent on Binance Agent OS.

Show BTCUSDT tool call + balance.  
Then: Open 10x BTC perpetual → **VETUM: DENY** and **no write tool**.

## 0:25–0:50  terminal

```powershell
npx tsx agent/run.ts 'Buy $800 of BNB on spot'
npx tsx agent/run.ts 'Open 10x BTC perpetual'
```

ALLOW then DENY `FORBIDDEN_VENUE`.

## 0:50–1:20  http://localhost:3000/app

Four chips + **HALT** → DENY HALTED.

## 1:20–1:30  /skill or GitHub

> Two agents. One check_policy. Clone and npm test — 9/9.

Skip the landing page if time is tight.
