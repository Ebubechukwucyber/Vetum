import { SiteChrome } from '@/components/SiteChrome'; import { demoPolicy } from '@/lib/demoPolicy';
const rules=[['Venue','spot only','If this fires: futures die.'],['Size','$10,000 max notional','If this fires: $50,000 > $10,000.'],['Human gate','$2,500 confirmation threshold','If this fires: a human decides.'],['Symbols','BNBUSDT · BTCUSDT · ETHUSDT','If this fires: unsupported symbols stop.'],['Withdrawals','false','If this fires: no agent can withdraw.'],['Loss','$1,500 daily loss limit','If this fires: autonomy is bounded.']];
export default function Constitution(){return <><SiteChrome active="/constitution"/><main className="mx-auto max-w-[1200px] px-5 py-16"><p className="font-mono text-xs tracking-[.2em] text-mist">THE CONSTITUTION</p><h1 className="mt-5 font-serif text-6xl leading-none md:text-8xl">Human-readable laws for autonomous exchange agents.</h1><div className="mt-16 grid gap-px border border-line bg-line md:grid-cols-2">{rules.map(([a,b,c])=><article key={a} className="bg-ink p-7"><p className="font-mono text-xs text-violet">{a}</p><h2 className="mt-12 font-serif text-3xl">{b}</h2><p className="mt-4 text-sm text-mist">{c}</p></article>)}</div><pre className="mt-12 overflow-auto border border-line bg-ink p-6 text-xs text-mist">{JSON.stringify(demoPolicy,null,2)}</pre></main></>}
