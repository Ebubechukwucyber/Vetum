'use client'; import { useState } from 'react'; import { SiteChrome } from '@/components/SiteChrome'; const rows=[['ALLOW','BNBUSDT','$800','Within constitution.'],['CONFIRM','ETHUSDT','$2,800','Notional requires a human.'],['DENY','BTCUSDT','$50,000','MAX_NOTIONAL'],['DENY','BTC perpetual','10×','FORBIDDEN_VENUE']];
export default function Ledger(){const [filter,setFilter]=useState('ALL');const [replay,setReplay]=useState<string|null>(null);const data=rows.filter(r=>filter==='ALL'||r[0]===filter);return <><SiteChrome active="/ledger"/><main className="mx-auto max-w-[1200px] px-5 py-16"><p className="font-mono text-xs tracking-[.2em] text-mist">AUDIT EXPLORER</p><h1 className="mt-5 font-serif text-6xl leading-none md:text-8xl">Every decision leaves a reason.</h1><div className="mt-10 flex gap-2">{['ALL','ALLOW','CONFIRM','DENY'].map(x=><button key={x} onClick={()=>setFilter(x)} className="border border-line px-3 py-2 font-mono text-xs text-mist">{x}</button>)}</div><div className="mt-8 overflow-hidden border border-line">{data.map((r,i)=><button onClick={()=>setReplay(r[1])} key={i} className="grid w-full grid-cols-4 border-b border-line p-5 text-left text-sm odd:bg-ink even:bg-void"><b>{r[0]}</b><span>{r[1]}</span><span className="font-mono">{r[2]}</span><span className="text-mist">{r[3]}</span></button>)}</div>{replay&&<p className="mt-5 font-mono text-xs text-violet">REPLAY LOADED · {replay} · intent → plan → simulation → policy</p>}</main></>}
